<Window x:Class="BarterPOS.ProductSearchWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Item Search"
        Height="460"
        Width="760"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Item Search"
                   FontSize="24"
                   FontWeight="Bold"
                   Margin="0,0,0,16"/>

        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="120"/>
            </Grid.ColumnDefinitions>

            <TextBox x:Name="SearchTextBox"
                     Padding="10,8"
                     FontSize="14"
                     VerticalContentAlignment="Center"/>

            <Button Grid.Column="1"
                    Content="Search"
                    Margin="10,0,0,0"
                    Click="Search_Click"/>
        </Grid>

        <DataGrid x:Name="ProductsGrid"
                  Grid.Row="2"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  MouseDoubleClick="ProductsGrid_MouseDoubleClick">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ItemNum" Binding="{Binding Code}" Width="160"/>
                <DataGridTextColumn Header="Product" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Price" Binding="{Binding Price}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button Content="Cancel"
                    Width="100"
                    Margin="0,0,10,0"
                    Click="Cancel_Click"/>
            <Button Content="Add Item"
                    Width="110"
                    Click="AddItem_Click"/>
        </StackPanel>
    </Grid>
</Window>
